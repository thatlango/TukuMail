# TukuMail Desktop

The desktop client is a Tauri 2 shell over the same audited web client. This keeps the inbox UX and API contracts consistent while giving Windows, macOS and Linux native packaging. Platform-specific mail integrations can be introduced behind Tauri commands without forking the UI.
