# TukuMail staging/small-node search backend.
# Keeps Apache James independent from OpenSearch while this service is validated
# on a single VPS. Replace with the James PostgreSQL search backend when the
# selected James release provides it as a stable production option.
implementation=scanning
